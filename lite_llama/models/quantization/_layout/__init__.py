"""Weight layout rearrangement per backend (private).

Placeholder for future backend-specific weight packing / reordering
(e.g. Marlin interleaved layout, AWQ GEMM packing, Triton block layout).
Each backend gets its own module: ``_layout/<backend>.py``.
"""
