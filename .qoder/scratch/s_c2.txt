### lite_llama/kernels/ops/__init__.py | total=71 | doc=19L/974c | usage=True
DOC| Operator layer: kernels grouped by domain, each group owning its rows.
DOC| 
DOC| This package answers "what does lite_llama compute?" — one directory per
DOC| operator domain (``gemm``, ``attention``, ``moe``, ``layernorm``, ``rope``,
DOC| ``activation``, ``sampling``, ``kvcache``, ``embeddings``), each group's
DOC| ``__init__.py`` holding every registration row for that op: native Triton
DOC| implementations and external-library contenders side by side as data. The
DOC| native implementations live beside their rows (``gemm/linear.py``,
DOC| ``attention/flashdecoding.py``, …); external adapters live in
DOC| :mod:`lite_llama.kernels.backend` and are referenced by string.
DOC| 
DOC| "Which row runs here?" is not this package's question —
DOC| :mod:`lite_llama.kernels.dispatcher` answers it. Importing this package
DOC| registers every row and never imports torch.
DOC| 
DOC| Usage:
DOC| ...(+2L)
VAR __all__

### lite_llama/kernels/ops/interfaces.py | total=482 | doc=27L/1365c | usage=True
DOC| Logical-operator ABCs: the call contracts behind each dispatch().
DOC| 
DOC| Each logical operator (ROADMAP foundation 2) owns exactly one stable ``op_id``
DOC| and one abstract call signature. KernelSpec rows declare *which* op they
DOC| implement; these ABCs define *how it is called*, so every backend picked by
DOC| :func:`~lite_llama.kernels.dispatcher.dispatch` can be dropped in without touching the
DOC| model code. The signatures follow the in-tree kernels, so the native rows point
DOC| straight at those kernels instead of at wrappers.
DOC| 
DOC| Design rules:
DOC|     * torch-free at runtime — torch appears only under ``TYPE_CHECKING`` so
DOC|       importing this module stays cold-start friendly (annotations are lazy).
DOC|     * A contract may be implemented by a plain function or a stateful class
DOC|       (external backends often hold buffers); dispatch never requires an
DOC|       isinstance check, the ABC is documentation plus a test hook.
DOC|     * ``elementwise.*`` is an open namespace: the ABC pins the loose shape
DOC| ...(+10L)
CLASS LogicalOp(ABC) :: Base of every logical-operator contract.
  - __call__()
CLASS AttentionPrefillOp(LogicalOp) :: Prefill attention over ragged (no-pad) batches.
  - __call__(q, k, v, sm_scale, b_start_loc, b_seq_len, max_seq_len)
CLASS AttentionDecodeOp(LogicalOp) :: Decode attention reading a paged KV cache.
  - __call__(q, k_cache, v_cache, qk_scale, b_req_tokens_table, b_req_idx, b_seq_len)
CLASS MlaDecodeOp(LogicalOp) :: Multi-head latent attention decode (placeholder until v0.10 wiring).
  - __call__(q, kv_cache, block_table, cache_seqlens)
CLASS LinearOp(LogicalOp) :: Dense ``x @ weight.T (+ bias)``, quantised or not.
  - __call__(x, weight)
CLASS MoeOp(LogicalOp) :: Routed expert FFN: ``sum_k w_k * SwiGLU_k(x) @ W2_k.T``.
  - __call__(hidden_states, w1, w2, topk_weights, topk_ids)
CLASS DispatchOp(LogicalOp) :: All-to-all token routing to expert ranks (EP).
  - __call__(x, topk_idx)
CLASS CombineOp(LogicalOp) :: Gather expert outputs back and merge the routed copies.
  - __call__(x, unsorted_src_idx, unsorted_weights)
CLASS RmsNormOp(LogicalOp) :: RMSNorm, optionally fused with the residual add (skip path).
  - __call__(x, residual, weight, eps)
CLASS RopeOp(LogicalOp) :: Rotary position embedding applied to query and key.
  - __call__(q, k, cos, sin)
CLASS KvWriteOp(LogicalOp) :: Scatter freshly computed K/V rows into the paged KV buffer.
  - __call__(k, v, select_index, kv_buffer)
CLASS SampleOp(LogicalOp) :: Token sampling core (top-k / top-p) over the final logits.
  - __call__(logits)
CLASS ElementwiseOp(LogicalOp) :: Pointwise transforms under the open ``elementwise.*`` namespace.
  - __call__(x)
def is_logical_op(op)

### lite_llama/kernels/ops/utils.py | total=67 | doc=11L/428c | usage=True
DOC| Shared helpers for the Triton kernels (block sizing, contiguity, HIP detection).
DOC| 
DOC| Small utilities the kernels reuse: ``calculate_settings`` picks BLOCK_SIZE /
DOC| num_warps, ``ensure_contiguous`` guards kernel inputs, plus a HIP check.
DOC| 
DOC| Incorporates code from Unsloth (Apache-2.0, https://github.com/unslothai/unsloth)
DOC| and Liger-Kernel; modifications by Yanning Chen, 2024.
DOC| 
DOC| Usage:
DOC|     block_size, num_warps = calculate_settings(n)
VAR MAX_FUSED_SIZE
def is_hip()
def keep(conf)
def ensure_contiguous(fn)
def calculate_settings(n)
VAR torch_to_triton_dtype

### lite_llama/kernels/ops/activation/__init__.py | total=36 | doc=13L/643c | usage=True
DOC| Activation domain: the SwiGLU pair, fused and split.
DOC| 
DOC| Two rows because the two entry points are genuinely different contracts:
DOC| ``elementwise.swiglu`` consumes a pre-concatenated fused gate/up tensor while
DOC| ``elementwise.swiglu_split`` takes gate and up separately — a caller holding
DOC| two tensors cannot serve the fused kernel and dispatch should say so, not have
DOC| the kernel silently transpose around it. No external row yet: activation
DOC| kernels are bandwidth-bound to the point where the win is in the surrounding
DOC| fusion, not in the elementwise op itself.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import activation  # noqa: F401  (registers rows)

### lite_llama/kernels/ops/activation/activations.py | total=57 | doc=8L/252c | usage=True
DOC| Elementwise activation kernels (Triton): relu, leaky_relu, tanh, gelu, silu.
DOC| 
DOC| Each is a thin pointwise Triton kernel over a flattened tensor — standalone and
DOC| useful on their own, kept apart from the fused MLP/attention kernels.
DOC| 
DOC| Usage:
DOC|     y = silu(x)
VAR sqrt2
def relu(x)
def leaky_relu(x)
def tanh(x)
def gelu(x)
def silu(x)

### lite_llama/kernels/ops/activation/swiglu.py | total=118 | doc=12L/505c | usage=True
DOC| SwiGLU activation as a fused Triton kernel.
DOC| 
DOC| Computes silu(gate) * up in one pass, reading both projection halves and writing
DOC| the product without a temporary in HBM. :func:`swiglu_forward` takes the halves
DOC| as two tensors; :func:`swiglu_forward_fused` reads them out of the single
DOC| ``[..., 2 * n_cols]`` tensor the merged gate/up GEMM produces, so the halves are
DOC| never split into separate allocations.
DOC| 
DOC| Usage:
DOC|     out = swiglu_forward(gate, up)
DOC|     out = swiglu_forward_fused(torch.cat([gate, up], dim=-1))
def _swiglu_forward_kernel(a_ptr, b_ptr, c_ptr, row_stride, n_cols, BLOCK_SIZE)
def swiglu_forward(gate, up)
def _swiglu_forward_fused_kernel(x_ptr, c_ptr, row_stride, n_cols, BLOCK_SIZE)
def swiglu_forward_fused(x)

### lite_llama/kernels/ops/attention/__init__.py | total=111 | doc=16L/927c | usage=True
DOC| Attention domain: the prefill/decode phases, KV write and MLA decode.
DOC| 
DOC| Two phases get separate op ids (``attention.prefill`` / ``attention.decode``)
DOC| because their shapes and kernels have nothing in common beyond the KV cache:
DOC| prefill is ragged batch GEMM, decode is paged single-token gather. The native
DOC| rows point straight at the Triton kernels; FlashInfer's rows are the first
DOC| externally-verified implementations and cover both phases with their own paged
DOC| KV pool (same ``kv:paged`` contract, different allocation). FlashMLA serves
DOC| ``attention.mla_decode`` — an op that has no native row at all, because MLA's
DOC| latent cache is a different layout, not a different size, and the tree has no
DOC| MLA model to host a Triton fallback (v0.11 wires the real thing; the single-
DOC| layer harness in ``models/mla_single_layer.py`` is the bench vehicle).
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import attention  # noqa: F401  (registers rows)

### lite_llama/kernels/ops/attention/flashattention2_nopad.py | total=260 | doc=13L/577c | usage=True
DOC| FlashAttention-2 (Triton) for variable-length, unpadded prefill batches.
DOC| 
DOC| Sequences are packed without padding and indexed by cumulative-length offsets.
DOC| Uses the v2 work partition (parallelise over query blocks, rescale softmax once).
DOC| 
DOC| ``sm_scale`` is the plain ``1 / sqrt(head_dim)``: the inner loop evaluates
DOC| ``exp2`` rather than ``exp``, and folding ``log2(e)`` into the scale is this
DOC| function's own business — the caller passes the same number every attention
DOC| backend takes.
DOC| 
DOC| Usage:
DOC|     out = flash_attention2_no_pad(q, k, v, sm_scale, b_start_loc, b_seq_len, max_seq_len)
VAR _LOG2E
VAR configs_tma
def keep_tma(conf)
def flash_attention2_nopad_kernel(Q, K, V, O, B_Start_Loc, B_Seqlen, sm_scale)
def flash_attention2_no_pad(q, k, v, sm_scale, b_start_loc, b_seq_len, max_seq_len)

### lite_llama/kernels/ops/attention/flashdecoding.py | total=407 | doc=10L/493c | usage=True
DOC| FlashDecoding: single-token decode attention against the paged KV cache.
DOC| 
DOC| Splits each sequence's KV history into partitions processed in parallel (stage 1),
DOC| then combines via a log-sum-exp reduction (stage 2). Supports both fp16 and fp8
DOC| (e4m3) KV cache with per-tensor dequantisation scales.
DOC| 
DOC| Usage:
DOC|     out = flash_decoding(q, k_cache, v_cache, scale, table, b_req_idx, b_seq_len, max_len)
DOC|     out = flash_decoding(q, k_fp8, v_fp8, scale, table, idx, lens, max_len, k_scale=0.5, v_scale=0.5)
def _flash_decoding_stage1_kernel(Q, K, V, qk_scale, k_scale, v_scale, b_req_tokens_table)
def flash_decode_stage1(q, k, v, qk_scale, b_req_tokens_table, b_req_idx, b_seq_len)
def _flash_decoding_stage2_kernel(Mid_O, Mid_O_LogExpSum, Ouput, mido_batch_stride, mido_heads_stride, mido_partitions_stride, mido_dim_stride)
def flash_decode_stage2(mid_o, mid_o_logexpsum, atten_output, b_seq_len, PARTITION_SIZE)
def flash_decoding(q, k_cache, v_cache, qk_scale, b_req_tokens_table, b_req_idx, b_seq_len)

### lite_llama/kernels/ops/embeddings/__init__.py | total=13 | doc=13L/534c | usage=True
DOC| Embedding domain: the vocab-parallel embedding lookup.
DOC| 
DOC| No registration rows: the lookup is bandwidth over a sharded table and the
DOC| module layer (``modules/vocab_parallel.py``) calls
DOC| :func:`~lite_llama.kernels.ops.embeddings.vocab_embedding.vocab_parallel_embedding`
DOC| directly — the dispatch question "which implementation?" has no meaningful
DOC| second answer until an external backend ships a fused variant worth ranking.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops.embeddings.vocab_embedding import (
DOC|         vocab_parallel_embedding,
DOC|     )

### lite_llama/kernels/ops/embeddings/vocab_embedding.py | total=119 | doc=18L/959c | usage=True
DOC| Vocabulary-parallel embedding lookup: gather this rank's rows, zero the rest.
DOC| 
DOC| Under tensor parallelism the embedding table is split along the vocabulary, so a
DOC| rank's contribution to a token's hidden vector is either its own row or exact
DOC| zeros — the ``all_reduce`` over ranks then sums exactly one real embedding per
DOC| token. This module is that contribution in a single kernel: the id->row mapping
DOC| (subtract the shard start, range-check) is two scalar register ops inside the
DOC| kernel, and the gather and the zeroing are one masked load/store pair.
DOC| 
DOC| The fused form replaces the eager chain of seven kernels (subtract, compare,
DOC| compare, and, clamp, gather, multiply) the lookup used to launch. Under TP there
DOC| are no CUDA graphs to hide those launches behind, so every one of them was paid
DOC| in full on every step; here the same arithmetic costs one launch plus the
DOC| collective.
DOC| 
DOC| Usage:
DOC| ...(+1L)
def _vocab_parallel_embedding_kernel(ids_ptr, weight_ptr, out_ptr, shard_start, local_vocab, stride_w, stride_o)
def _eager_embedding(flat, weight, shard_start, local_vocab)
def vocab_parallel_embedding(input_ids, weight, shard_start, local_vocab)

### lite_llama/kernels/ops/gemm/__init__.py | total=110 | doc=17L/853c | usage=True
DOC| GEMM domain: the linear projections, one row per quantisation scheme.
DOC| 
DOC| The group's whole surface is the five native Triton/torch entry points of
DOC| :mod:`~lite_llama.kernels.ops.gemm.linear` plus DeepGEMM's Hopper fp8 path.
DOC| Splitting linear by scheme (rather than one row with branches) is what lets
DOC| dispatch answer "which kernel for w8a16_fp8 on this shape?" without the kernel
DOC| itself re-deriving the answer internally.
DOC| 
DOC| DeepGEMM registers ``verified=False`` on purpose: its wrappers are written
DOC| against the upstream API but have no hardware run on record yet (the CI box is
DOC| an sm86 A10, DeepGEMM needs sm90+), so the golden gate keeps the row out of
DOC| default dispatch until an H100 box produces a max-abs-diff — an explicit
DOC| ``backend="deepgemm"`` may still force it.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import gemm  # noqa: F401  (registers the rows)

### lite_llama/kernels/ops/gemm/linear.py | total=134 | doc=17L/865c | usage=True
DOC| Entry points of the ``linear`` logical op: one per quantisation scheme.
DOC| 
DOC| The projection GEMM is the same operation in every model, but the numeric
DOC| format decides which kernel runs it. That format is a *dispatch key*, not a
DOC| runtime branch: :func:`~lite_llama.kernels.dispatcher` already knows the
DOC| scheme when it picks a row, so each function below may assume its own format
DOC| and map straight onto the Triton kernels in
DOC| :mod:`lite_llama.kernels.ops.quantization` — no ``if scheme ==`` chain anywhere on
DOC| the hot path. All of them share the
DOC| :class:`~lite_llama.kernels.ops.interfaces.LinearOp` signature so a caller
DOC| never has to know which one it got; scales it does not have are simply
DOC| ``None``.
DOC| 
DOC| Usage:
DOC|     y = linear_torch(x, weight, bias=bias)                       # unquantised
DOC|     y = linear_w8a16(x, qweight, weight_scale=scales, group_k=128)  # fp8/int8 weight
def linear_torch(x, weight)
def linear_w8a16(x, weight)
def linear_w4a16(x, weight)
def linear_w8a8_int8(x, weight)
def linear_w8a8_fp8(x, weight)

### lite_llama/kernels/ops/kvcache/__init__.py | total=24 | doc=11L/531c | usage=True
DOC| KV-cache domain: writing new tokens into the paged buffer.
DOC| 
DOC| One op, one row: ``kv_write`` has no backend politics — the buffer layout is
DOC| this repo's own ``[max_tokens, 2 * num_kv_heads, head_dim]`` allocation, and
DOC| any backend that wants to serve it has to write into exactly that. The
DOC| ``kv:paged`` tag is the contract; no dtype window because K/V arrive already
DOC| quantised when the cache is fp8, so uint8 rows are as legal here as bf16 ones.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import kvcache  # noqa: F401  (registers the row)

### lite_llama/kernels/ops/kvcache/update_kv_buffer.py | total=129 | doc=11L/499c | usage=True
DOC| Scatter freshly computed K/V rows into the paged KV buffer.
DOC| 
DOC| Writes each token's K and V into its allocated slot (``select_index``) in the
DOC| global cache buffer, where K occupies the first ``num_kv_heads`` rows and V the
DOC| second half. Taking the two projections as separate pointers — rather than a
DOC| ``torch.cat`` the caller would have to build per layer per step — keeps the
DOC| decode hot path free of an allocation plus two copies per layer.
DOC| 
DOC| Usage:
DOC|     update_kv_buffer(xk, xv, select_index, kv_buffer)
def _fwd_kernel_update_kv(K_Values, V_Values, Select_Index, KV_Buffer, stride_k_bs, stride_k_h, stride_k_d)
def update_kv_buffer(k, v, select_index, kv_buffer)

### lite_llama/kernels/ops/kvcache/update_kv_index.py | total=88 | doc=8L/268c | usage=True
DOC| Record where each sequence's newly written KV rows live.
DOC| 
DOC| Updates the per-request token->cache-slot index table so attention can gather a
DOC| sequence's KV history from the paged buffer.
DOC| 
DOC| Usage:
DOC|     update_kv_index(req_to_token_indexs, b_req_idx, b_seq_len, select_index)
def _fwd_kernel_update_kv_index(req_to_token_indexs, b_req_idx, b_seq_len, select_index, stride_req_to_token_b, stride_req_to_token_s)
def update_kv_index(req_to_token_indexs, b_req_idx, b_seq_len, select_index)

### lite_llama/kernels/ops/layernorm/__init__.py | total=49 | doc=11L/520c | usage=True
DOC| LayerNorm domain: RMSNorm, fused with the residual add when there is one.
DOC| 
DOC| One native row covers both the fused (residual) and plain paths — the kernel
DOC| picks between them on ``residual is None``, and a backend that only has the
DOC| plain one would be a different row, not a different flag. FlashInfer's row is
DOC| the per-layer glue case where an external library first beats the map: small,
DOC| latency-bound, called once per layer per pass.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import layernorm  # noqa: F401  (registers rows)

### lite_llama/kernels/ops/layernorm/skip_rmsnorm.py | total=139 | doc=8L/249c | usage=True
DOC| Fused residual-add + RMSNorm ("skip rmsnorm") in Triton.
DOC| 
DOC| Adds the residual and applies RMSNorm in a single pass, returning both the
DOC| normalised output and the updated residual.
DOC| 
DOC| Usage:
DOC|     out, residual = skip_rmsnorm(x, residual, weight, eps=1e-5)
def rms_norm_kernel(Y, X, W, y_stride_r, y_stride_c, x_stride_r, x_stride_c)
def skip_rms_norm_kernel(Y, X, R, W, y_stride_r, y_stride_c, x_stride_r)
def skip_rmsnorm(x, residual, weight, eps)

### lite_llama/kernels/ops/moe/__init__.py | total=57 | doc=12L/609c | usage=True
DOC| MoE domain: the fused expert GEMMs, one row per implementation.
DOC| 
DOC| The native row covers every quantisation scheme on purpose: ``fused_moe``
DOC| derives the expert format from ``w1.dtype`` (uint8 fp8-e4m3 / int8 / packed
DOC| int32) rather than from a flag, so splitting the row per scheme would be one
DOC| spec claim per branch of the same dispatch the kernel already does internally.
DOC| DeepGEMM's grouped variant is the sm90+ contender — same grouped-contiguous
DOC| semantics, Hopper tensor cores, unverified until it runs on real hardware.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import moe  # noqa: F401  (registers the rows)

### lite_llama/kernels/ops/moe/fused_moe.py | total=527 | doc=11L/448c | usage=True
DOC| Fused MoE: top-k routed experts as a Triton grouped GEMM.
DOC| 
DOC| Pipeline: moe_align_block_size -> GEMM1 (gate_up) -> silu_and_mul -> GEMM2
DOC| (down, router weight folded in) -> moe_sum. Supports fp16, fp8, int8, and int4
DOC| packed expert weights with group-wise scales.
DOC| 
DOC| Usage:
DOC|     out = fused_moe(hidden_states, w1, w2, topk_weights, topk_ids)
DOC|     out = fused_moe(x, qw1, qw2, tw, ids, w1_scale=s1, w2_scale=s2,
DOC|                     group_n=128, group_k=128)
VAR _QUANT_NONE
VAR _QUANT_FP8
VAR _QUANT_INT8
VAR _QUANT_INT4
VAR _QUANT_BLOCK_K
VAR _INT4_PACK_FACTOR
def moe_align_block_size(topk_ids, block_size, num_experts)
def _fused_moe_kernel(a_ptr, b_ptr, c_ptr, b_scale_ptr, b_zeros_ptr, topk_weights_ptr, sorted_token_ids_ptr)
def _launch_config(num_tokens, quant_mode)
def _invoke_moe_gemm(a, b, c, b_scale, b_zeros, topk_weights, sorted_token_ids)
def _silu_and_mul_kernel(x_ptr, out_ptr, stride_xm, N, BLOCK_N)
def _moe_sum_kernel(input_ptr, output_ptr, N, top_k, BLOCK_N)
def _quant_mode(weight, scale)
def fused_moe(hidden_states, w1, w2, topk_weights, topk_ids)

### lite_llama/kernels/ops/quantization/__init__.py | total=21 | doc=14L/730c | usage=False
DOC| Quantisation GEMM kernels: one Triton kernel per numeric format.
DOC| 
DOC| The weight-only kernels (w8a16, w4a16) keep the weight quantised in HBM and
DOC| widen it in the inner loop — the decode step is bound by how many bytes of
DOC| weight it must stream, so the saving is proportional to the compression ratio.
DOC| The W8A8 kernels (w8a8, fp8) quantise the activations too, so the whole GEMM
DOC| runs in the low precision.
DOC| 
DOC| Naming follows the *weight-activation* bit-width convention:
DOC|     w8a16 — 8-bit weight (fp8-e4m3 or int8), fp16 activation
DOC|     w4a16 — 4-bit weight (AWQ/GPTQ packed int4), fp16 activation
DOC|     w8a8  — int8 weight + dynamic per-token int8 activation (SmoothQuant)
DOC|     fp8   — fp8-e4m3 weight + dynamic per-token fp8-e4m3 activation
VAR __all__

### lite_llama/kernels/ops/quantization/fp8.py | total=237 | doc=10L/423c | usage=True
DOC| FP8 W8A8 GEMM: fp8-e4m3 weights + dynamic per-token fp8-e4m3 activations.
DOC| 
DOC| True W8A8: both operands are fp8 in the GEMM. On sm89+ native fp8 tensor cores
DOC| are used; on sm86 (A10) both operands are widened to fp16 by bit surgery inside
DOC| the loop. Activation is quantised per-token on the fly.
DOC| 
DOC| Usage:
DOC|     qx, x_scale = quantize_fp8_per_token(x)
DOC|     y = fp8_matmul(qx, x_scale, qweight, weight_scale_inv, group_n=1, group_k=K)
VAR _FP8_BIT_TRICK_SCALE_SQ
def _fp8_matmul_kernel(a_ptr, b_ptr, c_ptr, a_scale_ptr, b_scale_ptr, bias_ptr, M)
def _launch_config(num_tokens)
def fp8_matmul(qx, x_scale, qweight, weight_scale_inv)

### lite_llama/kernels/ops/quantization/w4a16.py | total=241 | doc=23L/1071c | usage=True
DOC| W4A16 GEMM: 4-bit weights (AWQ/GPTQ), fp16 activations, fp32 accumulation.
DOC| 
DOC| AWQ and GPTQ checkpoints pack 8 int4 values into each int32 word along the K
DOC| dimension, with one fp32 scale (and zero point) per group of ``group_size``
DOC| input channels. The kernel unpacks the int4 nibbles, applies the group-wise
DOC| dequantisation, and multiplies by the fp16 activation — all inside the GEMM
DOC| loop, so the weight never exists at fp16 in HBM.
DOC| 
DOC| v0.5 rewrite: uses ``tl.dot`` for tensor-core acceleration (SM80+ fp16 HMMA).
DOC| Each k-iteration processes one full group (group_size elements = BLOCK_K):
DOC|   1. Load [BLOCK_N, group_size//8] packed int32 words
DOC|   2. Unpack to [group_size, BLOCK_N] fp16 via bit shift
DOC|   3. Apply dequant: (nibble - zero) * scale
DOC|   4. ``tl.dot(a_tile, b_tile)`` accumulates on tensor cores
DOC| 
DOC| Packing order (AWQ/GPTQ standard):
DOC| ...(+6L)
VAR _PACK_FACTOR
def _w4a16_matmul_kernel(a_ptr, b_ptr, c_ptr, scale_ptr, zero_ptr, bias_ptr, M)
def w4a16_matmul(x, qweight, scales, zeros)

### lite_llama/kernels/ops/quantization/w8a16.py | total=244 | doc=10L/443c | usage=True
DOC| W8A16 GEMM: 8-bit weights (fp8-e4m3 or int8), fp16 activations, fp32 accumulation.
DOC| 
DOC| The weight stays 8-bit into the multiply-accumulate loop and is widened to fp16
DOC| one tile at a time. Two formats share one kernel via the IS_FP8 constexpr:
DOC| fp8-e4m3 (Qwen/DeepSeek checkpoints, bit-surgery dequant) and symmetric int8
DOC| (per-channel or group-wise, produced at load time).
DOC| 
DOC| Usage:
DOC|     y = w8a16_matmul(x, qweight, scales, group_n=128, group_k=128)
VAR FP8_E4M3_BIT_TRICK_SCALE
VAR FP8_BLOCK
def dequant_fp8e4m3(q)
def _w8a16_matmul_kernel(a_ptr, b_ptr, c_ptr, scale_ptr, bias_ptr, M, N)
def _launch_config(num_tokens)
def w8a16_matmul(x, qweight, scales)

### lite_llama/kernels/ops/quantization/w8a8.py | total=265 | doc=22L/935c | usage=True
DOC| SmoothQuant W8A8 GEMM: int8 weights + dynamic per-token int8 activations.
DOC| 
DOC| SmoothQuant migrates the quantization difficulty from activations to weights by
DOC| applying a mathematically equivalent transformation that smooths the activation
DOC| distribution. At inference time:
DOC| 
DOC| * **Weights** are pre-quantised to int8 with per-channel scales (static).
DOC| * **Activations** are quantised on-the-fly to int8 with per-token scales (dynamic).
DOC| 
DOC| The GEMM then runs as ``int8 @ int8 -> int32 -> fp16``, which is faster than
DOC| fp16 @ fp16 on Ampere+ GPUs that have int8 tensor cores.
DOC| 
DOC| Unlike w8a16 (which keeps activations at fp16), SmoothQuant quantises both
DOC| operands, so the kernel must:
DOC| 1. Compute per-token activation scales on the fly.
DOC| 2. Quantise activations to int8.
DOC| ...(+5L)
def _quantize_activations_kernel(x_ptr, q_ptr, scale_ptr, M, K, stride_xm, stride_xk)
def _smoothquant_matmul_kernel(a_ptr, b_ptr, c_ptr, a_scale_ptr, b_scale_ptr, bias_ptr, M)
def _launch_config(num_tokens)
def smoothquant_matmul(x, qweight, weight_scales)

### lite_llama/kernels/ops/rope/__init__.py | total=54 | doc=12L/572c | usage=True
DOC| RoPE domain: applying rotary position embeddings in place.
DOC| 
DOC| The native kernel takes the position ids the engine already computed; the
DOC| FlashInfer row does the same with its own CUDA implementation. Both are
DOC| verified against each other, and both rank below a hypothetical measured
DOC| winner until the frozen store says otherwise — position embedding is cheap
DOC| enough that the wrong choice is invisible in a profile and only shows up in
DOC| aggregate, which is exactly what the ranking tier is for.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import rope  # noqa: F401  (registers rows)

### lite_llama/kernels/ops/rope/rope_emb.py | total=162 | doc=14L/686c | usage=True
DOC| Rotary position embedding (RoPE) applied to q and k in a fused Triton kernel.
DOC| 
DOC| Rotates each head's channel pairs by the per-position angle in place, consuming the
DOC| ``(cos, sin)`` tables built by :mod:`lite_llama.modules.rotary_embedding`.
DOC| 
DOC| Each token is located through ``stride(0)`` alone, so q and k may be *column slices of
DOC| a fused qkv buffer*: their heads still sit next to each other, only the distance from
DOC| one token to the next is larger. Such a tensor is rotated where it lies instead of being
DOC| copied out and back, which is what keeps the fused QKV projection
DOC| (:class:`~lite_llama.modules.linear.QKVParallelLinear`) a net win.
DOC| 
DOC| Usage:
DOC|     q, k = rope_emb_forward(q, k, cos, sin)
def _triton_rope_emb(q_ptr, q_row_stride, k_ptr, k_row_stride, cos, cos_b_stride, cos_s_stride)
def _rows_are_contiguous(tensor)
def rope_emb_forward(q, k, cos, sin)

### lite_llama/kernels/ops/sampling/__init__.py | total=36 | doc=12L/585c | usage=True
DOC| Sampling domain: turning logits into token ids.
DOC| 
DOC| ``sample`` deliberately has no native row — the engine's sampler
DOC| (``engine/sampler.py``) already runs on the TP-sharded vocab with repetition
DOC| penalty, and a second implementation would be a second place sampling could
DOC| diverge between ranks. The FlashInfer row exists because its top-k/top-p
DOC| kernels are what a fused-sampling future would dispatch to, and registering
DOC| the op keeps the contract pinned even while the engine owns the default path.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.ops import sampling  # noqa: F401  (registers the row)

