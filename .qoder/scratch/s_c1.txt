### lite_llama/kernels/__init__.py | total=91 | doc=24L/1155c | usage=True
DOC| Kernel layer: implementations in ``ops/``, policy in ``dispatcher/``.
DOC| 
DOC| Three layers, one direction of knowledge:
DOC| 
DOC| * :mod:`lite_llama.kernels.ops` — what lite_llama computes. One directory per
DOC|   operator domain, each holding the native Triton implementations beside the
DOC|   registration rows that put them (and the external-library contenders) into
DOC|   the registry as data.
DOC| * :mod:`lite_llama.kernels.dispatcher` — which row runs here. Torch-free
DOC|   machinery (spec rows, registry, deterministic dispatch, autotune store).
DOC| * :mod:`lite_llama.kernels.backend` — one package per external library:
DOC|   install metadata, an availability probe, and the adapters that translate
DOC|   the op contracts into each library's calling convention.
DOC| 
DOC| This facade re-exports exactly what the model and engine layers call: the
DOC| kernels they invoke directly (the ``linear_*`` entries, paged attention,
DOC| ...(+7L)
VAR __all__

### lite_llama/kernels/backend/__init__.py | total=36 | doc=19L/950c | usage=True
DOC| One package per external kernel backend: metadata, probe and wrappers.
DOC| 
DOC| A backend package is pure data until its library is actually needed:
DOC| :class:`~lite_llama.kernels.dispatcher.registry.OpRegistry` rows in the ops
DOC| groups reference these packages' ``available()`` probes by string, and
DOC| :func:`~lite_llama.kernels.dispatcher.dispatch` calls the probe before
DOC| considering a row. What these packages export is three things — the install
DOC| recipe (``INSTALL``), the probe, and the kernel wrappers the rows' ``target``
DOC| strings resolve to (lazily, so importing this package never touches a
DOC| third-party library).
DOC| 
DOC| ``native`` is deliberately not a backend package: the native implementations
DOC| live beside their rows in :mod:`lite_llama.kernels.ops` and are the floor every
DOC| op falls back to.
DOC| 
DOC| Usage:
DOC| ...(+2L)
VAR __all__

### lite_llama/kernels/backend/probe.py | total=136 | doc=33L/1907c | usage=True
DOC| Can this machine run an external backend — and if not, how would it get it?
DOC| 
DOC| Every non-native KernelSpec must name an ``available`` probe (enforced by
DOC| :meth:`~lite_llama.kernels.dispatcher.spec.KernelSpec.validate`), and
DOC| :func:`~lite_llama.kernels.dispatcher.dispatch` calls it before considering
DOC| the row. That is what makes a missing wheel a *ranking* event instead of a
DOC| crash: the row drops out, ``explain`` says why, and the native Triton floor
DOC| serves the call. This module holds both halves of that answer —
DOC| :func:`library_present`, the probe those rows point at, and
DOC| :class:`BackendInstall`, the recipe to report when a backend is absent but
DOC| wanted.
DOC| 
DOC| Core design: the probe really imports the library rather than asking
DOC| ``importlib.util.find_spec``. These backends are compiled extensions built
DOC| against one CUDA (DeepGEMM, FlashMLA, DeepEP) or JIT toolchains that only fail
DOC| on first touch, so "the package directory exists" and "the package loads on
DOC| ...(+16L)
VAR logger
VAR EXTERNAL_BACKENDS
def library_present(module)
CLASS BackendInstall() :: How one external backend gets onto a machine, as data.
  - __post_init__()
  - how_to_get_it()
def survey()

### lite_llama/kernels/backend/deepep/__init__.py | total=45 | doc=25L/1319c | usage=True
DOC| DeepEP: expert-parallel all-to-all dispatch and combine.
DOC| 
DOC| DeepEP is what makes ``comm.dispatch`` and ``comm.combine`` real. Those two
DOC| contracts have no native row on purpose: MoE in this repo is *tensor* parallel,
DOC| every rank runs every expert over a slice of the intermediate dimension, so
DOC| there is no expert-parallel group to all-to-all across and the local permute is
DOC| already ``fused_moe``'s ``moe_align_block_size``. The EP group and these rows
DOC| land together (M2.5) — a placeholder before that would have been untestable.
DOC| 
DOC| Planned domains: ``comm.dispatch`` and ``comm.combine`` through DeepEP's buffer
DOC| interface, whose masked-GEMM output layout must line up with what the DeepGEMM
DOC| MoE row expects; the two declare that contract to each other in layout tags
DOC| rather than agreeing informally.
DOC| 
DOC| Unlike the other three, availability is not only about the import: these kernels
DOC| need several GPUs and an initialised EP group. The import probe is the honest
DOC| ...(+8L)
VAR INSTALL
def available()

### lite_llama/kernels/backend/deepgemm/__init__.py | total=53 | doc=26L/1428c | usage=True
DOC| DeepGEMM: fp8 dense GEMM and grouped MoE GEMM on Hopper.
DOC| 
DOC| DeepGEMM is the fp8 specialist — where the native path quantises around Triton
DOC| kernels, DeepGEMM's ``fp8_gemm_nt`` and grouped variants are written for the
DOC| Hopper tensor cores directly. Its rows live in
DOC| :mod:`~lite_llama.kernels.ops.gemm` (dense linear) and
DOC| :mod:`~lite_llama.kernels.ops.moe` (grouped); the wrappers they resolve to are
DOC| ``deepgemm.linear`` / ``deepgemm.moe``, with ``deepgemm.quant`` holding the
DOC| per-token-group fp8 quantisation both GEMMs need.
DOC| 
DOC| Two facts shape its metadata. It wants NT weights with per-128 block scales,
DOC| so the rows declare those layout tags and the transpose is cached once instead
DOC| of being assumed inside the kernel; and it JIT-compiles on first call, so the
DOC| benchmark harness must warm up before timing and the cache directory
DOC| (``DG_JIT_CACHE_DIR``, ``$HOME/.deep_gemm`` by default) belongs in the runbook.
DOC| 
DOC| ...(+9L)
VAR CUDA_SM90
VAR PROBE
VAR INSTALL
def available()

### lite_llama/kernels/backend/deepgemm/linear.py | total=83 | doc=19L/1072c | usage=False
DOC| DeepGEMM dense fp8 GEMM — the ``deepgemm/fp8_gemm_nt`` row's wrapper.
DOC| 
DOC| The contract is :class:`~lite_llama.kernels.ops.interfaces.LinearOp`'s superset
DOC| signature; the weight arrives the way native fp8 checkpoints store it
DOC| (``[N, K]`` uint8 e4m3 bit patterns plus ``weight_scale`` in the native block
DOC| convention), and the wrapper owns turning that into DeepGEMM's NT operand:
DOC| 
DOC| * the transpose-and-quantise result is cached per weight tensor
DOC|   (``data_ptr``-keyed, shape-checked) — inference weights never change, so the
DOC|   NT form is built exactly once, which is what the row's ``weight:nt`` /
DOC|   ``scale:block_128`` layout tags promise;
DOC| * activations are quantised per-token-group-128 on the fly, the same
DOC|   "quantisation is part of the format" contract ``linear_w8a8_fp8`` follows
DOC|   (native per-token scales are too coarse for DeepGEMM's ``[m, k // 128]``).
DOC| 
DOC| The row is ``verified=False``: this wrapper is written against the upstream
DOC| ...(+2L)
def _nt_weight(weight, weight_scale, group_n, group_k)
def fp8_gemm_nt(x, weight)

### lite_llama/kernels/backend/deepgemm/moe.py | total=129 | doc=24L/1309c | usage=False
DOC| DeepGEMM grouped fp8 MoE — the ``deepgemm/grouped_fp8_moe`` row's wrapper.
DOC| 
DOC| The contract is :class:`~lite_llama.kernels.ops.interfaces.MoeOp`. The native
DOC| ``fused_moe`` permutes tokens with Triton and runs both expert GEMMs in one
DOC| kernel family; here the same computation is expressed as two
DOC| ``m_grouped_fp8_gemm_nt_contiguous`` calls over a 128-aligned grouped layout:
DOC| 
DOC| 1. tokens are grouped per expert (stable sort over the flattened
DOC|    ``topk_ids``), each expert's segment padded to a multiple of
DOC|    ``ALIGNMENT`` because the contiguous grouped kernel requires aligned
DOC|    segment starts (``m_indices`` marks which expert owns each padded row);
DOC| 2. gate/up GEMM on the grouped activations, SwiGLU, down GEMM —
DOC|    per-token-group-128 activations, cached NT fp8 experts;
DOC| 3. the padded rows quantify to zero and are dropped by the gather-back, so
DOC|    padding never leaks into the weighted reduce.
DOC| 
DOC| ...(+7L)
VAR ALIGNMENT
def _nt_experts(w, w_scale, group_n, group_k)
def grouped_moe(hidden_states, w1, w2, topk_weights, topk_ids)

### lite_llama/kernels/backend/deepgemm/quant.py | total=153 | doc=14L/700c | usage=False
DOC| Per-token-group fp8 quantisation for the DeepGEMM GEMMs.
DOC| 
DOC| DeepGEMM's kernels take fp8 operands with fp32 scales: activations are
DOC| per-token-per-128-group (scales ``[m, k // 128]``), weights are 128x128
DOC| blockwise (scales ``[n // 128, k // 128]``). The native w8a8 format is
DOC| per-token x per-channel, whose scale shapes do not line up with either, so
DOC| these helpers live in this backend package rather than in
DOC| :mod:`lite_llama.kernels.ops.quantization`.
DOC| 
DOC| The whole backend is ``verified=False`` — no Hopper box has produced a
DOC| max-abs-diff yet — so the granularities here follow what the upstream kernel
DOC| requires; numerical equivalence with the native path gets checked when the
DOC| golden comparison freezes.
VAR FP8_E4M3_MAX
VAR GROUP_SIZE
def per_token_group_quant_fp8(x, group_size)
def block_quant_fp8_nt(weight, group_n, group_k)
def dequant_fp8_blocks(qweight, weight_scale, group_n, group_k)
def nt_block_fp8_from_checkpoint(qweight, weight_scale, group_n, group_k)

### lite_llama/kernels/backend/flashinfer/__init__.py | total=46 | doc=20L/939c | usage=True
DOC| FlashInfer: the wheel-installable external backend, Ampere onward.
DOC| 
DOC| FlashInfer is the first external backend on purpose — it is the only one of
DOC| the four that installs from a wheel and runs on Ampere, so it is what proves
DOC| the whole chain (filter, rank, explain, fall back) on the CI A10 rather than
DOC| only on Hopper.
DOC| 
DOC| The registration rows live in the ops groups they serve
DOC| (:mod:`~lite_llama.kernels.ops.attention`,
DOC| :mod:`~lite_llama.kernels.ops.layernorm`, :mod:`~lite_llama.kernels.ops.rope`,
DOC| :mod:`~lite_llama.kernels.ops.sampling`); this package holds the metadata
DOC| every one of those rows shares — the install recipe, the availability probe
DOC| and the capability window — plus the kernel wrappers under
DOC| ``flashinfer.attention`` / ``norm`` / ``rope`` / ``sample`` that the rows'
DOC| ``target`` strings resolve to.
DOC| 
DOC| ...(+3L)
VAR CUDA_SM75
VAR PROBE
VAR INSTALL
def available()

### lite_llama/kernels/backend/flashinfer/attention.py | total=162 | doc=29L/1380c | usage=False
DOC| FlashInfer attention wrappers: both phases behind the native signatures.
DOC| 
DOC| The dispatch contract is the native kernels' signature, so these wrappers are
DOC| where FlashInfer's plan/run model gets folded into the ragged-batch and
DOC| paged-pool calling conventions lite_llama already uses:
DOC| 
DOC| * :func:`prefill_attention` mirrors
DOC|   ``flash_attention2_no_pad``: packed ragged batch, causal, GQA from the
DOC|   head-count ratio.
DOC| * :func:`decode_attention` mirrors ``flash_decoding``: one token per sequence,
DOC|   slot table (``b_req_tokens_table``/``b_req_idx``) into the paged buffer.
DOC| 
DOC| Two API facts shape the decode wrapper. FlashInfer's plan takes the softmax
DOC| scale (and the run call does not), so ``qk_scale`` lands in ``plan``; and the
DOC| run call accepts the K and V pools as a *pair* of page tensors, which lets the
DOC| module layer's half-views of the engine's joint
DOC| ...(+12L)
VAR _WORKSPACE_BYTES
VAR _prefill_wrapper
VAR _decode_wrapper
def _reset_cache()
def _get_workspace()
def _get_wrapper(which)
def prefill_attention(q, k, v, sm_scale, b_start_loc, b_seq_len, max_seq_len)
def decode_attention(q, k_cache, v_cache, qk_scale, b_req_tokens_table, b_req_idx, b_seq_len)

### lite_llama/kernels/backend/flashinfer/norm.py | total=43 | doc=14L/768c | usage=False
DOC| FlashInfer RMSNorm wrapper behind the native signature.
DOC| 
DOC| The contract (:class:`~lite_llama.kernels.ops.interfaces.RmsNormOp`) returns a
DOC| pair — ``(normalised, residual)`` — with the second element being the summed
DOC| input on the fused path and ``x`` itself on the plain one. FlashInfer's plain
DOC| ``rmsnorm`` is functional (returns the normalised tensor), while
DOC| ``fused_add_rmsnorm`` is in-place over its two arguments (reading each before
DOC| writing both), so the wrapper's whole job is to make it look like the same
DOC| functional pair: clone both operands before the kernel consumes them, hand
DOC| back the pair the decoder layer threads into the next norm without branching.
DOC| 
DOC| Usage (from a spec row's ``target``):
DOC|     from lite_llama.kernels.backend.flashinfer.norm import rmsnorm
def rmsnorm(x, residual, weight, eps)

### lite_llama/kernels/backend/flashinfer/rope.py | total=41 | doc=17L/937c | usage=False
DOC| FlashInfer RoPE wrapper behind the native signature.
DOC| 
DOC| The contract (:class:`~lite_llama.kernels.ops.interfaces.RopeOp`) is
DOC| table-driven: the module layer computes ``cos``/``sin`` for the batch's
DOC| geometry and hands them over. FlashInfer's ``apply_rope`` is varlen- and
DOC| offset-driven — packed ``q``/``k``, batch ``indptr``, per-sequence position
DOC| ``offsets``, tables derived internally from ``rope_theta`` — so the wrapper
DOC| rebuilds those from the table geometry: this repo's tables cover contiguous
DOC| positions 0..seq-1 per sequence, which is ``indptr = arange * seq_len`` with
DOC| zero offsets. The rotation bases (``rope_theta``) must match too: the contract
DOC| carries no theta, so the row's golden record is measured on the default
DOC| 10000-base models and a theta-divergent model would surface as a golden
DOC| failure, not a silent wrong rotation.
DOC| 
DOC| Usage (from a spec row's ``target``):
DOC|     from lite_llama.kernels.backend.flashinfer.rope import rope
def rope(q, k, cos, sin)

### lite_llama/kernels/backend/flashinfer/sample.py | total=38 | doc=11L/535c | usage=False
DOC| FlashInfer sampling wrapper behind the native signature.
DOC| 
DOC| The contract (:class:`~lite_llama.kernels.ops.interfaces.SampleOp`) is the
DOC| sampler's kernel core: temperature, top-k, top-p, one token per row. The
DOC| deterministic (greedy) path is plain argmax on purpose — golden runs and the
DOC| argmax-parity check need both implementations to agree exactly, and routing
DOC| greedy through a sampling kernel would only add a place to disagree.
DOC| 
DOC| Usage (from a spec row's ``target``):
DOC|     from lite_llama.kernels.backend.flashinfer.sample import sample
def sample(logits)

### lite_llama/kernels/backend/flashmla/__init__.py | total=52 | doc=25L/1305c | usage=True
DOC| FlashMLA: Multi-head Latent Attention decode on Hopper.
DOC| 
DOC| FlashMLA is the only backend here that serves an op with no in-tree kernel at
DOC| all: ``attention.mla_decode`` exists as a contract precisely so this row has
DOC| something to satisfy. The row lives in
DOC| :mod:`~lite_llama.kernels.ops.attention` and resolves to the wrapper in
DOC| ``flashmla.mla_decode`` — ``flash_mla_with_kvcache`` with ``get_mla_metadata``
DOC| called once per batch before the decode loop, a tile-scheduler handle, so the
DOC| row's target is the wrapper that owns that handle, not the raw kernel.
DOC| 
DOC| MLA keeps a *latent* KV cache (``c_kv`` of shape ``[Skv, L_kv]``) rather than
DOC| per-head K and V, which is a different layout, not a different size: the row
DOC| declares its own ``kv:mla_latent`` tag so it can never be dispatched against
DOC| the per-head paged pool the native rows use. Since the tree has no MLA model,
DOC| the single-layer harness (``models/mla_single_layer.py``) is what verifies it
DOC| — numerics versus a pure-PyTorch reference plus per-op latency. Wiring a real
DOC| ...(+8L)
VAR CUDA_SM90
VAR PROBE
VAR INSTALL
def available()

### lite_llama/kernels/backend/flashmla/mla_decode.py | total=77 | doc=23L/1127c | usage=False
DOC| FlashMLA decode — the ``flashmla/mla_decode`` row's wrapper.
DOC| 
DOC| The contract is
DOC| :class:`~lite_llama.kernels.ops.interfaces.MlaDecodeOp` — an op with no native
DOC| row at all: MLA's latent cache (``kv_cache`` pages are ``[page_size,
DOC| kv_lora_dim]``, no head axis) is a different layout from the per-head paged
DOC| pool the native attention rows share, and the tree has no MLA model to host a
DOC| Triton fallback. FlashMLA is the only implementation.
DOC| 
DOC| Two shape conversions, both views: ``q`` ``[bsz, heads, qk_dim]`` becomes
DOC| ``[bsz, 1, heads, qk_dim]``, and the latent cache gains its implicit single KV
DOC| head — ``[pages, page, lkv]`` → ``[pages, page, 1, lkv]``.
DOC| 
DOC| ``get_mla_metadata`` is the tile-scheduler handle: the wrapper recomputes it
DOC| per call from ``cache_seqlens`` (one small kernel) and feeds it to
DOC| ``flash_mla_with_kvcache``, so callers never manage a handle across the
DOC| ...(+6L)
def mla_decode(q, kv_cache, block_table, cache_seqlens)

### lite_llama/kernels/dispatcher/__init__.py | total=75 | doc=23L/1115c | usage=True
DOC| The dispatch tier: choose an implementation, once, and be able to say why.
DOC| 
DOC| Three questions, three tiers (ROADMAP foundation 2): the kernels themselves
DOC| live in :mod:`lite_llama.kernels.ops` (grouped by operator domain, each group
DOC| registering its rows as data), the external libraries live in
DOC| :mod:`lite_llama.kernels.backend`, and *which implementation runs here* is
DOC| answered by :func:`dispatch` in this package — deterministically, with the
DOC| decision cached per :class:`DispatchKey` and the full rejection chain available
DOC| through ``explain``.
DOC| 
DOC| Importing this package never imports torch: a spec is strings and frozen
DOC| dataclasses, implementations are referenced as ``"module:attr"`` strings and
DOC| loaded only when first dispatched. :mod:`autotune` lives beside the mechanism
DOC| because it is the perf half of the same decision — the frozen store it keeps
DOC| is what the ranking step reads once wired in.
DOC| 
DOC| ...(+6L)
VAR __all__

### lite_llama/kernels/dispatcher/dispatch.py | total=389 | doc=25L/1306c | usage=True
DOC| Deterministic dispatch: filter, rank, cache — and be able to say why.
DOC| 
DOC| ``dispatch(op, ...)`` answers "which implementation runs here?" in four fixed
DOC| steps (ROADMAP foundation 2, pillar 5):
DOC| 
DOC| 1. **Filter** — availability probe, capability window, dtype, scheme, hard
DOC|    shape constraints, layout tags, and the golden-accuracy gate. Every
DOC|    rejection is recorded with its reason, verbatim.
DOC| 2. **Rank** — frozen perf measurements (``perf_key``, fastest first) beat
DOC|    shape preferences, which beat static priority; the spec name is the final
DOC|    tie-break so equal inputs always yield equal outputs.
DOC| 3. **Cache** — the decision for a full :class:`DispatchKey` is computed once.
DOC| 4. **Report** — ``explain`` renders the decision chain for humans;
DOC|    ``LITE_LLAMA_KERNEL_TRACE=1`` emits one JSON line per decision for tools.
DOC| 
DOC| An explicit ``backend=`` (or the ``LITE_LLAMA_*_BACKEND`` env vars) narrows the
DOC| ...(+8L)
VAR FORCE_BACKEND_ENV
VAR TRACE_ENV
def op_backend_env(op)
def _forced_backend(op, explicit)
CLASS DispatchKey() :: Everything a decision depends on; equal keys must give equal answers.
  - shape_dict()
CLASS Selected() :: The outcome of one :func:`dispatch` call, with its full audit trail.
  - load()
  - explain()
def resolve_target(ref)
def _probe_available(ref)
def dtype_label(dtype)
VAR PerfProvider
def _no_measurements(spec, key)
def set_perf_provider(provider)
def _perf_latency_ms(spec, key)
def invalidate_cache(op)
def _reject_reason(spec, key)
def _rank_key(spec, key)
def _rank_text(spec, key)
def dispatch(op)
def explain(op)
def _trace(decision)

### lite_llama/kernels/dispatcher/registry.py | total=111 | doc=13L/598c | usage=True
DOC| Global KernelSpec registry: one table for every implementation of every op.
DOC| 
DOC| Registration happens once, at import of the impl modules, and never touches
DOC| torch — a spec is strings and frozen dataclasses. The registry owns the table,
DOC| its invariants (unique names, lookup helpers) and the per-op decision cache
DOC| that :mod:`lite_llama.kernels.dispatcher.dispatch` fills in.
DOC| 
DOC| Usage:
DOC|     from lite_llama.kernels.dispatcher.registry import REGISTRY, register
DOC| 
DOC|     register(KernelSpec(name="native/linear_torch", op="linear", ...))
DOC|     REGISTRY.implementations("linear")   # -> specs, native floor included
VAR logger
CLASS OpRegistry() :: The process-wide table of :class:`KernelSpec` entries.
  - __init__()
  - register(spec)
  - notify_change(op)
  - implementations(op)
  - spec(name)
  - ops()
  - specs()
  - decisions()
  - native_floor(op)
VAR REGISTRY
def register(spec)

### lite_llama/kernels/dispatcher/spec.py | total=272 | doc=26L/1244c | usage=True
DOC| Declarative KernelSpec: one implementation's full dispatch contract, torch-free.
DOC| 
DOC| Everything the deterministic dispatcher (:mod:`lite_llama.kernels.dispatcher.dispatch`)
DOC| needs to filter, rank and explain an implementation lives here as pure data:
DOC| hardware window, dtypes, quantisation schemes, shape constraints, layout tags
DOC| and the golden-accuracy gate. Registrations are plain strings and dataclasses,
DOC| so importing this module — and registering every impl at startup — never loads
DOC| torch, keeping cold start at seconds (ROADMAP foundation 2, pillar 4).
DOC| 
DOC| Field provenance: ``capability``/``available`` follow sglang's KernelSpec
DOC| shape; ``shape``/``layout``/``golden`` are lite_llama originals — layout
DOC| changes become explicit, cacheable and visible in ``explain`` instead of being
DOC| silent assumptions inside a kernel.
DOC| 
DOC| Usage:
DOC|     spec = KernelSpec(
DOC| ...(+9L)
VAR ConstraintKind
VAR _TARGET_RE
VAR _OP_RE
CLASS ShapeConstraint() :: One predicate over a named dimension of the dispatch shape key.
  - satisfied_by(dims)
  - __str__()
CLASS ShapeRequirement() :: Hard feasibility gates plus soft preferences over the dispatch shape.
  - is_feasible(dims)
  - preference_score(dims)
CLASS LayoutRequirement() :: Input layout tags an implementation insists on, as data.
  - missing_from(available)
  - satisfied_by(available)
CLASS GoldenRecord() :: Accuracy-gate status of an implementation against its reference.
CLASS KernelSpec() :: Full dispatch contract of one implementation of a logical op.
  - validate()
  - dtype_ok(dtype)
  - scheme_ok(scheme)
  - shape_ok(dims)
  - layout_missing(available)
VAR NATIVE_BASELINE
VAR UNMEASURED
VAR PAGED_KV_TAGS
VAR PAGED_KV
VAR NATIVE_BASELINE
VAR UNMEASURED
VAR PAGED_KV_TAGS
VAR PAGED_KV

### lite_llama/kernels/dispatcher/autotune/__init__.py | total=53 | doc=25L/980c | usage=False
DOC| Kernel autotune: collect, persist, and look up optimal tile configurations.
DOC| 
DOC| Public API:
DOC|     - :class:`TuneKey` — the stable key contract (gpu, op, shape_bucket, dtype).
DOC|     - :class:`ConfigStore` — JSON-backed persistent store.
DOC|     - :func:`get_best_config` — high-level lookup for kernel call sites.
DOC|     - :mod:`frozen` — frozen measured ranking: the store's answer to
DOC|       ``set_perf_provider`` (ROADMAP v0.10).
DOC| 
DOC| Usage (kernel call site)::
DOC| 
DOC|     from lite_llama.kernels.dispatcher.autotune import get_best_config
DOC| 
DOC|     config = get_best_config("fused_moe", m=num_tokens, n=N, k=K, dtype="fp16")
DOC|     if config is None:
DOC|         config = _launch_config(num_tokens, quant_mode)  # heuristic fallback
DOC| ...(+8L)
VAR __all__

### lite_llama/kernels/dispatcher/autotune/config_key.py | total=113 | doc=15L/700c | usage=False
DOC| Autotune key structure — the stable contract for v0.6 perf_key.
DOC| 
DOC| A :class:`TuneKey` uniquely identifies a (GPU, op, problem shape, dtype)
DOC| combination. It is the lookup key into the persisted JSON config store and
DOC| must remain backward-compatible: once a key is written, any future version
DOC| must still be able to load it.
DOC| 
DOC| Key design choices:
DOC|     - ``gpu``: normalised device name (spaces → underscores).
DOC|     - ``op``: kernel family identifier (e.g. ``"fused_moe"``).
DOC|     - ``shape_bucket``: ``M{m}_N{n}_K{k}`` where M is bucket-quantised.
DOC|     - ``dtype``: activation/weight dtype label (``"fp16"``, ``"int8"``, ``"int4"``).
DOC| 
DOC| M-bucketing keeps the cache manageable: decode batches 1–16 share one entry.
def bucket_m(m)
def make_shape_bucket(m, n, k)
def normalize_gpu_name(name)
CLASS TuneKey() :: Immutable identifier for one autotune entry.
  - to_dict()
  - from_dict(cls, d)
  - build(cls, op, m, n, k, dtype, gpu)

### lite_llama/kernels/dispatcher/autotune/config_store.py | total=136 | doc=14L/487c | usage=False
DOC| Persistent JSON config store for autotune results.
DOC| 
DOC| Each kernel op gets its own JSON file under the cache directory. The store
DOC| supports concurrent reads (lazy-loaded into memory) and append-style writes
DOC| (load → merge → flush). Thread-safety within a single process is ensured by
DOC| the GIL for the dict operations; cross-process safety relies on atomic rename.
DOC| 
DOC| File layout::
DOC| 
DOC|     ~/.cache/lite_llama/autotune/
DOC|     ├── fused_moe.json
DOC|     ├── flash_attn_nopad.json
DOC|     └── w4a16_matmul.json
VAR _SCHEMA_VERSION
VAR _DEFAULT_CACHE_DIR
CLASS ConfigStore() :: Read/write autotune configurations to disk as JSON.
  - __init__(cache_dir)
  - cache_dir()
  - get(key)
  - get_entry(key)
  - put(key, config, latency_us)
  - load_all(op)
  - _json_path(op)
  - _ensure_loaded(op)
  - _flush(op)

### lite_llama/kernels/dispatcher/autotune/frozen.py | total=165 | doc=29L/1722c | usage=False
DOC| Frozen measured ranking: the autotune store's answer to ``set_perf_provider``.
DOC| 
DOC| The dispatcher's rank step consults a pluggable ``PerfProvider`` before shape
DOC| preferences and static priority (see ``dispatch._rank_key``). This module is
DOC| the wiring ROADMAP v0.10's foundation-2 close-out asks for: latencies measured
DOC| once by ``benchmarks/kernels/freeze_dispatch_ranking.py`` are frozen into a
DOC| store, and :func:`install_frozen_perf_provider` turns that store into the
DOC| provider — so one dispatch key keeps selecting the same implementation until
DOC| the records are re-frozen, and an external backend flips a default only when a
DOC| measurement says it is faster (``UNMEASURED`` priority alone never does).
DOC| 
DOC| Record layout:
DOC| 
DOC| * Records live in a dedicated ``frozen/`` subdirectory of the autotune cache,
DOC|   so tile-config entries and frozen-rank entries never share a namespace.
DOC| * The scheme folds into the bucket string (``"unquantized@M16_N4096_K4096"``)
DOC| ...(+12L)
VAR FROZEN_DIR_NAME
VAR FROZEN_RANK_ENV
VAR _RECORD_KIND
def frozen_store(base_dir)
def frozen_bucket(scheme, dims)
def freeze_record(store)
def make_frozen_perf_provider(store)
def install_frozen_perf_provider(store)

### lite_llama/kernels/dispatcher/autotune/lookup.py | total=80 | doc=11L/536c | usage=False
DOC| High-level autotune lookup — the call sites' single entry point.
DOC| 
DOC| Kernel launchers call :func:`get_best_config` before falling back to their
DOC| heuristic ``_launch_config``. The function is intentionally cheap on miss
DOC| (returns ``None`` immediately) so the hot path pays no extra cost when no
DOC| tuned config has been collected yet.
DOC| 
DOC| The environment variable ``LITE_LLAMA_AUTOTUNE`` controls behaviour:
DOC|     - ``"0"``: disabled — always returns ``None`` (forces heuristic fallback).
DOC|     - ``"1"`` or unset: enabled — looks up the config store.
def _get_store()
def _get_gpu()
def get_best_config(op, m, n, k, dtype)
def reset()

### lite_llama/kernels/dispatcher/autotune/searcher.py | total=117 | doc=21L/763c | usage=True
DOC| Offline autotune searcher: benchmark kernel configs and persist the best.
DOC| 
DOC| The searcher runs a grid of tile configurations for a given kernel/shape pair,
DOC| measures each with CUDA events (warmup + repeated median), and writes the
DOC| winner into a :class:`~lite_llama.kernels.dispatcher.autotune.ConfigStore`.
DOC| 
DOC| Usage::
DOC| 
DOC|     from lite_llama.kernels.dispatcher.autotune import ConfigStore
DOC|     from lite_llama.kernels.dispatcher.autotune.searcher import AutotuneSearcher
DOC| 
DOC|     store = ConfigStore()
DOC|     searcher = AutotuneSearcher(store)
DOC|     best = searcher.search(
DOC|         op="fused_moe",
DOC|         shape=(16, 4096, 11008),
DOC| ...(+4L)
CLASS AutotuneSearcher() :: Benchmark a list of kernel configs and persist the best one.
  - __init__(store, warmup, repeat)
  - gpu()
  - search(op, shape, dtype, configs, run_fn)
  - _benchmark(run_fn, config)

